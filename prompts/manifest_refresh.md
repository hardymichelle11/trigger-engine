# Refresh the System Manifest

Read the recent code changes and update `docs/history/SYSTEM_MANIFEST.md`.

## Refresh these sections
- Architecture changes (section 2, if layout changed)
- Code & Configurations (section 4, if new modules/files)
- Inflight tasks (section 6, mark completed, add new)
- File tree (section 8, if files added/removed)
- Changelog (section 11, add dated entry)
- Step-by-step setup (section 7, if backfill commands changed)

## Do NOT rewrite stable sections unless they changed
- Summary (section 1) — only if capabilities added
- Database schema (section 5) — only if tables changed
- GCP config (section 9) — only if project/dataset changed
- Cost summary (section 10) — only if symbol count changed
