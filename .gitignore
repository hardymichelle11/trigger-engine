# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets & credentials
service-account*.json
sa-key*.json
*-credentials.json
.env
.env.*
!.env.example

# ThetaData local Terminal — never commit jar (39 MB, redistributable
# from operator's account), creds.txt (contains email + password), or
# the auto-generated config.toml (operator-machine-specific, may capture
# host/port/region settings tied to the local install).
ThetaTerminalv3.jar
creds.txt
config.toml
*.jar

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
__pycache__/
