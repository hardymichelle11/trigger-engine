# Session Notes — 2026-04-10

## Goals
1. Add CORZ (Core Scientific) and IREN (Iris Energy) to ticker catalog and pipeline
2. Build Credit-Volatility Options Engine (12-section retail hedge fund model)
3. Integrate 2026 options watchlist (top-27 active + historical top-100)
4. Create setup registry system to externalize setup definitions
5. Establish Claude task lane (WORK_QUEUE, DECISIONS, SESSION_NOTES)

## What got built

### Session 1: Credit-Vol Engine + Watchlist

**New files created:**
- `src/signalEngine.js` — 12-section options engine (regime, scoring, timing, strikes, probability, profit mgmt, roll logic, sentiment, trade recommendations)
- `src/CreditVolScanner.jsx` — Full React UI with regime panel, setup cards, detail panel, position manager, income tracker, 7 scan filter buttons
- `src/optionsWatchlist.js` — 2026 top-27 + historical top-100 with spread quality, wheel suitability, tier metadata, scan filters

**Ticker catalog expanded:** 22 → 48 symbols
- Added: CORZ, IREN, BX, APO, ARCC, OWL, OBDC, COIN, HYG, KRE, LQD, SPY, QQQ, SPX, TLT, SLV, GLD, XLF, XLE, TSLA, GOOG, AMD, PLTR, MSTR, SMCI, META, HOOD, BTDR, FXI

**Pipeline synced:** Both market_data_pipeline.py and refresh_schedule.py updated with all symbols.

**TickerSetupBuilder fixed:** Removed duplicate hardcoded catalog, now imports from tickerCatalog.js.

**Navigation:** 3-page app (Trigger Engine ↔ Credit-Vol Scanner ↔ Setup Builder).

### Session 2: Setup Registry + Task Lane

**New files created:**
- `src/config/setups.js` — All 7 setup definitions in normalized format
- `src/lib/setupRegistry.js` — Registry with backward-compat conversion, symbol extraction, runtime mutations
- `src/lib/setupValidator.js` — Schema validation per setup type
- `docs/WORK_QUEUE.md` — Prioritized task list (P1/P2/P3)
- `docs/DECISIONS.md` — Architecture decision log
- `docs/SESSION_NOTES/2026-04-10.md` — This file
- `docs/CLAUDE.md` — Operating rules for Claude in this project

**App.jsx refactored:** Removed ~70 lines of inline SETUPS/MARKET_REGIME/getAllSymbols; imports from registry.

## Decisions made
- Setup registry over inline definitions (enables AI-safe edits, builder linkage)
- Options watchlist as metadata overlay, not merged into ticker catalog
- Two separate scanning engines, not one merged engine
- See docs/DECISIONS.md for full rationale

### Session 3: Sprint A — Setup Builder → Runtime

- Wired TickerSetupBuilder to live setup registry and React state
- Builder validates → `addSetup()` → React state update → scanner re-renders
- localStorage persistence for runtime-added setups
- Toggle/remove controls in builder UI
- Lifted setup state to main.jsx; App.jsx accepts `engineSetups` prop
- React owns render state, registry is mutation layer, config is seed data

### Session 4: Sprint B — BQ Rolling Refresh

- Replaced DELETE+INSERT with staging table + MERGE (streaming-buffer-safe)
- `upsert_bars()`: load job to staging → MERGE on (symbol, exchange, ts)
- `validate_bq.py`: duplicate detection, row counts, staging health
- quotes_live remains append-only (unchanged)

## Decisions made
- Setup registry over inline definitions (enables AI-safe edits, builder linkage)
- Options watchlist as metadata overlay, not merged into ticker catalog
- Two separate scanning engines, not one merged engine
- React owns render state, registry is mutation layer
- Staging + MERGE over DELETE+INSERT for BQ refresh
- See docs/DECISIONS.md for full rationale

## Build status (end of day)
- Final build: 38 modules, 313.37 KB (90.92 KB gzip)
- 50 test assertions across 3 suites (setups, engine, watchlist)
- All P1 items complete
