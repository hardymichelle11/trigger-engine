# Session Notes — 2026-04-12

## Session 19: Regime Context Calibration

- Wired `regimeContext` into `buildUiCard` — every card now carries regime, bias, regimeScore, confidence, vixState, earlyStress, componentScores
- `calibrationTracker.js` captures `regimeContext` in calibration snapshots (backward-compatible, nullable)
- Added regime-grouped stats: `getCalibrationStats()` returns breakdowns by regime, confidence, earlyStress, high regimeScore
- Quarterly report generates natural-language regime-conditioned recommendations
- CSV export includes 6 regime columns
- 30 new test assertions (76 total in calibration suite)

## Session 20: Knowledge Bot + Vertex AI

- Built knowledge bot with 4 modes: OFF, FAQ_ONLY, SEARCH_ONLY, FULL_CHAT
- Budget guardrails with automatic downgrade: FULL_CHAT → SEARCH_ONLY (80%) → FAQ_ONLY (95%)
- Usage tracker with daily/monthly request limits
- 14 FAQ entries across 5 categories + 12 glossary terms with keyword search
- KnowledgeBotPanel integrated into CreditVolScanner sidebar
- Vertex AI backend server (`server/knowledgeBotServer.js`) with Gemini 2.5 Flash
- Dashboard-specific system prompt for domain-accurate answers
- GCP project `supple-synapse-470605-c5` authenticated, Vertex AI API enabled
- Vite proxy for `/api/knowledgebot` → localhost:3001
- 76 test assertions

## Session 21: Landing Page

- Built full landing page with Tailwind CSS v4, framer-motion, lucide-react
- Redesigned to clean proportional slide layout (Fintax-inspired aesthetic)
- 6 full-viewport slides: Hero, Services, Dashboard Preview, Videos, Docs, AI Help
- Serif headings, alternating white/grey/navy backgrounds, generous whitespace
- App starts on landing page, "Open Dashboard" navigates to scanner
- `@tailwindcss/vite` plugin configured

## Session 22: Universal Live-State Recalibration

- Created `liveStateEngine.js` — universal recalibration framework for all setup types
- 3-layer architecture: setupDefinition → liveMarketState → derivedState
- Recalculation triggers: price drift (1.5%/2.5%), leader drift, leverage gap drift (0.5%), IV shift (10%), ATR shift (15%), regime change, new bar close, freshness TTL (90s), MC cache TTL (120s)
- `buildUiCard` attaches `liveState` with anchor price, dynamic targets, vol, freshness timestamp
- `renderSafeCardState` nulls out stale analytics (probability, recommendation, distT1Pct)
- `FreshnessBadge` in detail panel shows LIVE/AGING/STALE + anchor drift + regime
- `SetupCard` shows STALE/BLOCKED badges with hover tooltip
- 84 test assertions

## Session 23: Observability + Alert Safety

- Added `RECALC_REASON_CODES` enum with normalized codes
- `shouldRecalculate` returns `reasonCodes` array alongside human-readable reasons
- `buildLiveState` stores audit trail: `lastRecalcReasons`, `lastRecalcReasonCodes`
- `renderSafeCardState` returns `invalidation` object with reasons, suppressedFields, anchorDrift, ageSec
- `isAlertSafe()` — 6-check safety gate (liveState, stale, freshness, anchor drift, probability, MC anchor)
- Alert engine Gate 0: freshness safety blocks stale cards, returns `blockedByFreshness` + `freshnessReason`
- Telemetry: in-memory `_recalcLog` with debug mode toggle
- `HealthPanel` in scanner sidebar: live/aging/stale counts, blocked reasons, top recalc/invalidation drivers
- Per-card `blockReason` shown as BLOCKED badge with hover
- 135 → 172 test assertions in live-state suite

## Session 24: BQ Persistence for Ops Metrics

- Created `opsEventCollector.js` — localStorage-backed event buffer (5000 rolling max)
- 4 event types: recalc, invalidation, alert_block, alert_fire
- Wired into `liveStateEngine.js` (recalc + invalidation) and `alertEngine.js` (block + fire)
- Created `flush-ops-metrics.js` — drains events → JSON backup + BQ insert
- 3 BQ tables: `ops_recalc_events`, `ops_invalidation_events`, `ops_alert_events`
- `npm run ops:flush` for manual flush
- Windows Task Scheduler: `MarketData-OpsFlush` runs daily at 4:10 PM (after market close)
- 201 total assertions in live-state suite

## Build status
- 17 test suites, 746 total assertions, 0 failures
- Build clean (Vite production build)

## Decisions made
- Regime context is observation-only — no auto-tuning of weights yet
- Knowledge bot uses Gemini 2.5 Flash via Vertex AI (not Claude) for cost reasons
- Landing page defaults to Georgia serif + dark theme
- Live-state recalibration is universal across all setup types, not symbol-specific
- Ops metrics persist summarized events only, not raw per-refresh noise
- BQ flush runs once at market close, not on every refresh tick
